# Playwright Test Project

This project contains Playwright tests and supporting utilities.

## Structure

- src/ - application page objects, components, fixtures, utilities, API clients, and test helpers
- tests/ - Playwright test specs
- config/ - environment configuration files
- data/ - JSON/CSV test data
- auth/ - global setup/teardown and storage state
- pipelines/ - CI pipeline definitions
